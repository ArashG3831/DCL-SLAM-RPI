/**:
  ros__parameters:
    use_sim_time: true

    robot_base_frame: base_link

    costmap_topic: /map
    costmap_updates_topic: /map_updates

    visualize: true
    planner_frequency: 1.2
    progress_timeout: 6.0

    potential_scale: 1.2
    orientation_scale: 0.0
    gain_scale: 1.5

    transform_tolerance: 0.05
    min_frontier_size: 0.045

    return_to_init: false
